<!DOCTYPE html>
<html lang="en">
<%- include('partials/head', {page: 'Track'})  %>
<body>
    <div class="main">
        <%- include('partials/top-bar') %>
        <%- include('partials/nav-bar')  %>
        <div class="main-view">
            <div class="track-container">
                <div class="track-info-container">
                    <img src=<%= track.image %> alt=image-<%= track.title %>>
                    <div class="track-info-content">
                        <p>SONG</p>
                        <h1><%= track.title %></h1>
                        <p><b><%= track.artists[0] %></b> • <%= track.duration %></p>
                    </div>
                </div>
                <div class="track-content">
                    
                </div>
                
            </div>
            

            
        </div>
        <%- include('partials/now-playing-bar')  %>
    </div>
</body>
</html>