<!DOCTYPE html>
<html lang="en">
<%- include('partials/head', {page: 'Track'})  %>
<body>
    <div class="main">
        <%- include('partials/top-bar') %>
        <%- include('partials/nav-bar')  %>
        <div class="main-view">
            <div class="track-info-container">
                <img src=<%= track.image %> alt="#">
                <p>SONG</p>
                <h1><%= track.title %></h1>
                <p><%= track.artists[0] %> <%= track.duration %></p>
            </div>
        </div>
        <%- include('partials/now-playing-bar')  %>
    </div>
</body>
</html>